# Command check docker
### Check evironment
`whoami`
### Check Linux
`cat /etc/issue`
### Check git
`git --version`
### Check package
`which fastapi`
### Check Path and list
`ls -lah`

### Update
`sudo apt-get update`

